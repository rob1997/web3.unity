name: Duplicate Samples 🪞

on:
  workflow_dispatch:

jobs:
  setup:
    name: Setup job
    uses: rob1997/web3.unity/.github/workflows/setup.yaml@main
    with:
      arguments: " -duplicate_samples"
    secrets: inherit
